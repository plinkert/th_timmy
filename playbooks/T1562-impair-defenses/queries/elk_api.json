{
  "query": {
    "bool": {
      "filter": [
        { "range": { "@timestamp": { "gte": "{{timestamp_start}}", "lte": "{{timestamp_end}}" } } },
        { "query_string": { "query": "Disable-WindowsDefender OR Set-MpPreference OR auditpol OR DisableRealtimeMonitoring" } }
      ]
    }
  }
}
